# Vercel部署故障排除指南

## 🔧 连接问题解决方案

### 问题1：GitHub连接失败
**症状**：无法连接GitHub仓库或导入项目

**解决方案**：
1. **检查GitHub授权**：
   - 访问 GitHub Settings → Applications → Authorized OAuth Apps
   - 确保Vercel有访问权限
   - 如没有，重新授权：访问 vercel.com 登录时选择GitHub授权

2. **手动连接仓库**：
   ```bash
   # 确保项目已推送到GitHub
   git remote -v  # 检查远程仓库地址
   git push origin main  # 推送最新代码
   ```

3. **使用GitHub CLI**：
   ```bash
   # 安装GitHub CLI
   brew install gh
   
   # 重新认证
   gh auth login
   ```

### 问题2：账户不匹配
**症状**：IDE账户与Vercel账户不一致

**解决方案**：
1. **统一账户登录**：
   - 退出当前Vercel账户
   - 使用与GitHub相同的账户重新登录
   - 清除浏览器缓存和Cookie

2. **隐私模式**：
   - 使用浏览器隐私模式访问vercel.com
   - 重新进行GitHub授权

## 🚀 替代部署方案

### 方案1：手动部署（推荐）
1. **构建项目**：
   ```bash
   npm run build
   ```

2. **拖拽部署**：
   - 访问 https://vercel.com/dashboard
   - 点击 "New Project"
   - 选择 "Import Git Repository" 或 "Deploy from Git"
   - 直接拖拽构建好的 `dist` 文件夹

### 方案2：CLI部署
1. **安装Vercel CLI**：
   ```bash
   npm i -g vercel
   ```

2. **登录并部署**：
   ```bash
   vercel login  # 使用GitHub账户登录
   vercel --prod  # 部署到生产环境
   ```

### 方案3：其他平台（备选）
如果Vercel仍有问题，可以考虑：

#### Netlify部署
```bash
# 安装Netlify CLI
npm install -g netlify-cli

# 登录并部署
netlify login
netlify deploy --prod
```

#### GitHub Pages部署
1. 在GitHub仓库设置中启用GitHub Pages
2. 选择部署源为GitHub Actions
3. 创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    - name: Install dependencies
      run: npm ci
    - name: Build
      run: npm run build
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

## 📋 部署检查清单

### 部署前准备
- [ ] 项目已推送到GitHub
- [ ] package.json中包含正确的build脚本
- [ ] vercel.json配置文件已创建
- [ ] 项目能在本地成功构建

### 部署步骤
1. **访问Vercel**：https://vercel.com
2. **选择项目源**：Import Git Repository
3. **配置项目**：
   - Framework Preset: Vite
   - Build Command: npm run build
   - Output Directory: dist
4. **环境变量**：如有需要，添加环境变量
5. **部署**：点击Deploy按钮

### 部署后验证
- [ ] 网站能正常访问
- [ ] 所有功能正常工作
- [ ] 移动端适配良好
- [ ] 加载速度正常

## 🎯 面试展示建议

### 如果部署遇到问题
**不要慌张**，可以这样表达：

"我目前完成了项目的核心功能开发，包括物理仿真引擎、交互式界面和智能评测系统。由于一些账户授权的技术细节，部署环节还在处理中，但这不影响项目本身的技术价值和功能完整性。"

### 强调技术亮点
1. **核心算法**：物理仿真引擎的实现
2. **架构设计**：PhET SceneryStack的应用
3. **用户体验**：教育科技的专业设计
4. **代码质量**：TypeScript和现代前端最佳实践

### 展示方式
- **本地演示**：直接运行 `npm run dev` 展示
- **代码讲解**：重点讲解核心算法和架构设计
- **功能演示**：分模块展示三个主要功能
- **技术文档**：展示README和代码注释

记住，**项目的核心价值在于技术实现和教育意义**，部署只是锦上添花！